[package]
name = "cobuild-types"
version = "0.1.0"
edition = "2021"

[features]
default = ["entity"]
entity = ["dep:molecule"]

[dependencies]
molecule = { version = "0.9.2", default-features = false, optional = true }
