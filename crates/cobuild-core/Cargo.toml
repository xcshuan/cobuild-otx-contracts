[package]
name = "cobuild-core"
version = "0.1.0"
edition = "2021"

[dependencies]
blake2b-ref = "0.3.1"
cobuild-types = { path = "../cobuild-types" }
molecule = { version = "0.9.2", default-features = false }
