[package]
name = "cobuild-core"
version = "0.1.0"
edition = "2021"

[dependencies]
blake2b-ref = "0.3.1"
ckb-std = { version = "1.1", default-features = false, features = ["ckb-types", "dummy-atomic"] }
cobuild-types = { path = "../cobuild-types", default-features = false }
